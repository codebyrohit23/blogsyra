// import { z } from 'zod';

// export const resetPassswordSchema = z.object({
//   token: z.string().nonempty('Reset password token is required'),
//   newPassword: z
//     .string()
//     .min(8, 'Password must be at least 8 characters')
//     .regex(passwordRegex, passwordRegexMessage),
// });

// export type resetPassswordInput = z.infer<typeof resetPassswordSchema>;
