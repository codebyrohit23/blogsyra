// import { z } from 'zod';

// export const verifyForgotPasswordOtpSchema = z.object({
//   email: z.string().nonempty('Email address is required').email('Invalid email address'),
//   deviceId: z.string().nonempty('deviceId  is required'),
//   otp: z.string().min(6, 'Enter a valid otp'),
// });

// export type verifyForgotPasswordlOtpInput = z.infer<typeof verifyForgotPasswordOtpSchema>;
