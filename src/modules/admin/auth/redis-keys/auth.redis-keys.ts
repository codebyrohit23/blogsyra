// import { buildKey } from '@/core/cache';

// export const AURH_REDIS_KEYS = {
//   blacklist: (jti: string) => buildKey('auth', 'blacklist', jti),

//   session: (sessionId: string) => buildKey('auth', 'blacsessionklist', sessionId),
// };
