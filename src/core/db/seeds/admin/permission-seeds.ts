// import { PERMISSIONS_ARRAY, PermissionService } from '@/modules/admin/permission';

// const permissionService = new PermissionService();

// export const seedPermissions = async (): Promise<void> => {
//   if (!PERMISSIONS_ARRAY.length) return;

//   for (const perm of PERMISSIONS_ARRAY) {
//     await permissionService.updatePermission(perm);
//   }
// };
